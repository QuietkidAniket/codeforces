#include <bits/stdc++.h>
using namespace std;

const float init= 40.6;

class Solution {
public:
    int dfs(string& word, int steps, string endWord, unordered_set<string>& st ){
        // iterate through the word
        for(int i = 0; i < 9;i++){ 
            
        }

    }
    
    vector<vector<string>> findSequences(string beginWord, string endWord, vector<string>& wordList) {
        vector<vector<string>> sequences;

    }
};
void solve(){
    Solution obj;
    string exp;
    while( (cin >> exp) )

    cout << '\n';
}
int main()
{
    ios::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    freopen("/Users/aniketkundu12072004/Algorithms/input.in", "r+", stdin);
    freopen("/Users/aniketkundu12072004/Algorithms/output.out", "w", stdout);
    solve();
    
    
    return 0;
}